# DeepSeek API (required)
DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_MODEL=deepseek-chat

# Database (local dev defaults — change DB_HOST to localhost if running backend outside Docker)
DB_HOST=postgres
DB_PORT=5433
DB_USER=jobhunter
DB_PASSWORD=jobhunter
DB_NAME=jobhunter
